
%\documentclass{book}
%\documentclass{report}
\documentclass{scrbook}
\usepackage{expex,expex-chapno}

\lingset{exnotype=chapter.arabic}

\begin{document}

\chapter{Blah}

First give an example:

\pex<one>
\a<A> first
\a<B> second
\xe

\ex \deftagex{goop}\dots\xe


\chapter{Blah-Blah}

Compare (\getfullref{one.B}), which is part \getref{one.B} in
problem (\getref{one}), with the second part of (\nextx)
or with (\anextx)

\pex
\a third
\a fourth
\xe

Just to make sure the count is incremented:

\ex \dots\xe

And the deftag problem is fixed, see (\getref{goop}).

\end{document}


